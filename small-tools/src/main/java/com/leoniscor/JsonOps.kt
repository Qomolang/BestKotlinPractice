package com.leoniscor

/**
 * @author gaosong
 */
class JsonOps {


    /**
     * 比较两个json是否相同，第一步，将两个json的key按照相同顺序排序
     * 注意：依赖fastjson
     */
//    fun sortJsonByKey(input: String) {
//        var jsonObject = JSON.parseObject(input);
//        input = jsonObject.toString(SerializerFeature.MapSortField);
//        println("input = " + input);
//    }

}